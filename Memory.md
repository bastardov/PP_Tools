# Memory — память по проекту PP_Tools

Подробное знание о плагине. `AGENTS.md` — общие правила, этот файл — конкретика по
инструментам и модулям. Цель: новый агент, прочитав `AGENTS.md` + `Memory.md`, быстро
понимает проект и может работать без перечитывания всех `script.py`.

Как пользоваться:
- Перед доработкой инструмента читать его карточку ниже.
- После изменений обновлять соответствующий раздел.
- Держать только устойчивое знание (что делает инструмент, какие модули `lib` и параметры
  использует, ключевые решения). Не копировать сюда весь код и не писать одноразовые детали.

---

## Общая структура

- Таб: `PP_ОВиК.tab` — единственный таб плагина.
- Панели именуются с числовым префиксом (`01_`, `02_`, …), который задаёт порядок в ленте.
- Каждый инструмент — папка `*.pushbutton` с `script.py`, `icon.png`, обычно `bundle.yaml`.
- Связанные кнопки группируются в `*.stack` (стек из 2 кнопок).
- Порядок кнопок внутри панели может задаваться файлом `layout` (есть в `03_Марки.panel`).
- Общая логика вынесена в `lib/` (см. раздел «Модули lib»).
- Платформа: pyRevit, Revit 2022, IronPython 2.7. Ограничения по синтаксису — в `AGENTS.md`.

---

## Модули lib

Общие модули, переиспользуемые инструментами. Подключаются из `script.py` (через `sys.path`
pyRevit видит `lib/`).

### `pp_settings.py` + `pp_settings.json`
Чтение/запись настроек плагина. `DEFAULT_SETTINGS` задаёт значения по умолчанию, реальные
значения хранятся в `pp_settings.json`. Используется панелью «Настройки» и инструментами,
которым нужны параметры. Ключевые параметры:
- `show_success_report`, `show_warning_report` — показывать ли отчёты.
- `pipe_pair_distance_mm` (600.0) — расстояние для пары труб.
- `detect_orientation`, `heatloss_show_report` — флаги для теплопотерь/ориентации.
- `mep_transfer_show_pyrevit_report` — показывать подробный pyRevit-отчёт после
  инструмента «Передача параметров по MEP».
- `sheet_name_prefixes` — префиксы имён листов (для «Заполнить имя листа по видам»).
- `sheet_plan_location_map`, `sheet_plan_section_map` — словари формата
  `"ключ = значение"` для «Заполнить имя листа по планам» (локации и разделы).
- `proxy_rule_codes`, `proxy_position_types` — справочники для прокси-контроллера.
- `tag_rules` — таблица правил для «Инструментов меток» (см. `pp_tagrules.py`). Список
  словарей: `cat` (имя BuiltInCategory элемента), `family`, `type` (или null), `tag_family`,
  `tag_type`, `leader`. Стартово засеяны пресеты пользователя (SR, РВ-1, PPK-2… и др.).
- `tag_size_presets`, `tag_size_active` — размерные правила меток (2026-08-20). Пресет:
  `{"name": ..., "rules": [...]}`, строка правила: `cat`, `min_len`, `max_len` (null =
  «и больше»), `tag_family`, `tag_type`. `tag_size_active` — имя пресета, по которому
  работает «Применить по шаблону».

> При добавлении нового параметра: добавить в `DEFAULT_SETTINGS`, при необходимости — в UI
> панели «Настройки», и описать здесь.

**Безопасность файла настроек (2026-08-24).** Формат имеет служебный ключ
`_schema_version` (`SETTINGS_SCHEMA_VERSION` в `pp_settings.py`). Известные ключи при
чтении проверяются по типам значений из `DEFAULT_SETTINGS`; неверный тип заменяется
дефолтом и записывается в `_logs/settings.log`, а импорт файла с неверными типами
отклоняется. Сохранение идёт сначала во временный UTF-8 JSON в той же папке; файл
перечитывается для проверки, затем атомарно заменяет основной через .NET `File.Replace`.
Предыдущая рабочая версия хранится как `pp_settings.json.bak`; вне IronPython работает
восстанавливаемая схема из двух переименований. Повреждённый основной файл не затирается:
он переносится в `pp_settings.invalid_YYYYMMDD_HHMMSS.json`, после чего настройки
восстанавливаются из `.bak`, а если резервной копии нет — из дефолтов. Та же защита
срабатывает при прямом `save_settings()` без предварительного `load_settings()`.

### Автономная проверка проекта

- `.agents/validate_project.py` — единый Python 3 preflight без запуска Revit.
  Проверяет структуру вкладок/панелей/кнопок, обязательную телеметрию, bundle-файлы,
  PNG-иконки, совместимость с IronPython 2.7, синтаксис Python, XAML, ресурсы темы,
  `FindName`/биндинги и восстановление `pp_settings` на временных файлах.
- `.agents/run_checks.bat` запускает preflight через `py -3` или `python`.
- Обычный режим возвращает ненулевой код только для критических ошибок. Накопленное
  наследие (иконки 1024×1024, старые `forms.alert`, bare `except`, строки без `u` и т. п.)
  выводится как замечания. Ключ `--strict` делает замечания блокирующими — его включать
  по мере очистки проекта.
- `.agents/validate_wpf.py` остаётся библиотекой детальных WPF-проверок; разбор AST
  понимает допустимый для IronPython префикс `ur`, который не принимает Python 3.
- Проверка Revit API и поведения транзакций по-прежнему выполняется вручную через
  pyRevit Reload.

### `pp_tagrules.py`
Общая логика инструментов «Инструменты меток» и «Маркировать по правилу». Содержит
`ELEMENT_CATEGORIES` (4 категории: воздухораспределители/арматура возд./оборудование/арматура
труб + соответствующие категории марок `*Tags`). Ключевое:
- `find_rule(rules, cat_enum, family, type)` — поиск правила в два уровня: сначала точное
  `family+type`, затем правило на всё семейство (`type=None`).
- `resolve_tag_symbol(doc, cat_enum, tag_family, tag_type)` — находит `FamilySymbol` марки
  по именам семейства и типа; None, если не загружен в проект.
- `collect_element_types(doc, cat_enum)` / `collect_tag_symbols(doc, cat_enum)` — типы
  элементов и типы марок категории из проекта (для дерева и combo конфигуратора).
- `get_assignment/set_assignment/remove_assignment` — точечная работа с правилом узла.
- `CONFIG_ENUM_NAMES` — какие категории показывает конфигуратор. Сейчас 9:
  воздухораспределители, арматура возд., арматура труб, оборудование, воздуховоды,
  трубы, материалы изоляции труб/возд., обобщённые модели (у каждой своя категория
  марок `*Tags`). Для системных типов (воздуховоды/трубы/изоляция) имя семейства берётся
  из `ElementType.FamilyName`; если пустое — единый ярлык `NO_FAMILY` = «(без семейства)»
  (одинаково в конфигураторе и при применении, чтобы правило совпадало).
- `get_family_name/get_type_name/get_element_category_enum`, `get_location_point`.

**Размерные правила воздуховодов (2026-08-20).** У воздуховодов ширина марки зависит не от
типа элемента, а от длины надписи: «100х100» — 7 знаков, «1000х1000» — 9, «ДВ 10, 1000х500» —
15. Поэтому рядом с `tag_rules` живёт второй вид правил — диапазон длины ПЛЮС область
действия (семейство и тип воздуховода):
- `SIZE_ENUM_NAMES` — только `OST_DuctCurves`. Воздуховодов НЕТ в `CONFIG_ENUM_NAMES`:
  первая вкладка их не показывает, всё настраивается во вкладке «Воздуховоды».
- `measure_tag_text(text)` — длина самой длинной строки надписи. Текст берётся из
  `IndependentTag.TagText`, то есть формат label угадывать не нужно: что нарисовано на виде,
  то и меряем.
- `size_rule_scope(rule)` — область действия строки `(family, type)`; `(None, None)` — все
  воздуховоды. `scope_rank` — насколько строка точная.
- `rules_in_scope(rules, cat, family, type)` — строки, действующие на этот воздуховод, от
  точных к общим: тип → семейство → общая. Та же лестница, что и у `find_rule`.
- `find_size_rule(rules, cat, length, family, type)` — первая подходящая по длине строка
  в этом порядке. `has_size_rules` — есть ли вообще строки под категорию.
- `find_size_overlap(..., family, type)` — пересечение диапазонов проверяется ТОЛЬКО внутри
  одной области, поэтому «круглые 1–7» и «прямоугольные 1–7» уживаются.
- `size_coverage_gaps(rules, cat, family, type)` — непокрытые длины для выбранной области
  (учитывает и общие строки); окно показывает их строкой под списком.
- `strip_size_categories(rules)` — вычищает из `tag_rules` записи категорий, которые теперь
  настраиваются размерными правилами. Вызывается в кнопках 12 и 14: старые правила по
  воздуховодам удаляются (решение 2026-08-20, переносить не стали).
- `normalize_presets/normalize_size_rule/sort_size_rules/active_preset/active_size_rules` —
  чтение и приведение пресетов из настроек. Пустые настройки дают один пустой пресет
  `DEFAULT_PRESET_NAME`. Тип без семейства обнуляется: смысла не имеет.

> Замер делается ОДИН раз, до `ChangeTypeId`. Если у марок разной ширины различается набор
> label, после смены типа текст изменится, и повторный замер начнёт качать тип туда-обратно.

### `pp_tags.py`
Работа с марками (тегами) и их элементами. Содержит белый список категорий
`ALLOWED_CATEGORIES` (воздухораспределители, арматура воздуховодов, оборудование, арматура
трубопроводов) и `ALLOWED_CATEGORY_NAMES`. Хелперы получения элемента по марке
(`get_tagged_element_from_tag` и др.). Основной модуль для панели «Марки».

### `pp_geometry.py`
Геометрические хелперы поверх `Autodesk.Revit.DB`: операции с `XYZ` (`xyz_add`, `xyz_sub`),
получение опорной точки элемента (`get_element_anchor_point`) и т.п. Используется
инструментами, работающими с координатами (опуск, копирование вертикальных трасс, оформление).

### `pp_wall_merge.py`
Объединение кусков одной стены в одну стену (2026-09-02). Ядро кнопки «Объединить стены»
панели «Теплопотери»: разбор выделения на наборы (`plan`) и создание объединённой стены
(`merge`). Про WPF не знает. Каждый кусок считается прямоугольником в системе координат
набора (u — вдоль оси стены в плане, v — абсолютная отметка), объединение прямоугольников
считается по сетке из их координат, контур обводится по границе закрашенных клеток.

### `pp_leaders.py`
Работа с выносками марок: получение ссылок (`get_first_reference`), состояние конца выноски
(`get_leader_end_condition`: no_leader / free / …). Используется при копировании/сопоставлении
марок, где важно сохранить геометрию выноски.

### `pp_tag_layout.py`
Раскладка марок на виде: поиск наложений и раздвижка (2026-08-31). Ядро кнопки
«Раздвинуть марки», рассчитано на встраивание в другие кнопки панели.

- `collect_tags(doc, view)` — все марки, видимые на виде.
- `measure_boxes(doc, view, tags, movable_ids=None)` — габариты марок БЕЗ выносок.
  Открывает СВОЮ транзакцию-зонд и откатывает её, поэтому вызывать её при уже открытой
  транзакции нельзя. `movable_ids` — какие марки разрешено двигать; остальные попадают
  в расчёт препятствиями.
- `solve(boxes, gap, max_offset, mode, ...)` — чистая математика, про Revit не знает.
  Поэтому окно инструмента пересчитывает предпросмотр на каждое изменение параметров,
  не открывая транзакций.
- `apply_moves(doc, view, boxes, moves, options)` — перестановка голов марок ВНУТРИ уже
  открытой транзакции.
- `spread_tags(doc, view, tags, options)` — всё вместе, со своей транзакцией.
- `mm_to_ft` / `ft_to_mm` / `view_scale` — миллиметры НА ЛИСТЕ ↔ футы модели.
- `size_summary(boxes, view)` — замеренный габарит марки в мм на листе для строки-диагностики
  в окне.
- `leader_segments` / `leaders_cross` / `segment_hits_box` / `segment_push` — геометрия
  выносок в плоскости вида, без Revit API.
- `count_leader_problems(boxes, positions)` — перекрестившиеся выноски и выноски сквозь
  чужой текст.

> Выноски меряются ДО их выключения в зонде: после `HasLeader = False` геометрию уже не
> прочитать. У марок с прикреплённой выноской `GetLeaderEnd` не работает — конец берётся
> по точке расположения маркируемого элемента (`_element_point`).

> В расчёте выноска начинается не в точке головы, а у КРАЯ марки со стороны элемента —
> так её рисует Revit. Если считать от головы, отрезок идёт сквозь собственный текст
> марки, и проверка «выноска сквозь чужой текст» врёт на каждом столбике марок.

> `tag.get_BoundingBox(view)` возвращает габарит ВМЕСТЕ с выноской и для поиска наложений
> не годится. Замер идёт в транзакции-зонде: выноски выключаются, снимается чистый габарит
> текста, транзакция откатывается. Именно откат, а не «выключил → включил обратно»: он
> возвращает выноски, изгибы и `LeaderEndCondition` ровно как были.

> Считаем в плоскости вида (`RightDirection` / `UpDirection`), а не в мировых XYZ — тогда
> инструмент одинаково работает на планах, разрезах и фасадах.

### `pp_param_picker.py` + `pp_param_picker.xaml`
Общее окно выбора имени параметра (2026-08-19). Раньше жило внутри кнопки «Передача
параметров по MEP-соединениям» (`parameter_picker.xaml` + класс `ParameterPickerWindow`),
вынесено в `lib`, чтобы кнопка «Выбрать…» появилась везде, где имя параметра вводится руками.
- Вызов: `pp_param_picker.ask(options, current=..., title=..., subtitle=..., owner=..., empty_text=...)`.
  Возврат — имя параметра или `None` при отказе.
- Элемент списка: строка либо `{u"name": ..., u"display": ...}`. В поле уходит `name`,
  пользователь видит `display` — туда пишут откуда параметр и насколько покрывает выбранные
  категории.
- Про Revit не знает: список готовит инструмент. Поиск фильтрует по `name`, двойной щелчок
  и Enter выбирают, Esc закрывает. `owner` — вызвавшее окно (иначе главное окно Revit).
- Принцип: кнопка ставится РЯДОМ с полем ручного ввода, а не вместо него — встроенные и
  семейные параметры в списки привязок не попадают, их всё равно вводят руками.
- Подключён в: «Передача параметров по MEP-соединениям», «Окрасить рандомно»
  (через ключ `choice.picker` в `pp_paint_dialog`), «Проверка спецификации»,
  «Проверка Модели», «Проверка теплопотерь» (через `option_type=u"param"`).

### `pp_param_source.py`
Единственное место, где список параметров собирается из модели (2026-08-19). Нужен, чтобы
`pp_param_picker` остался Revit-независимым, а инструменты не повторяли обход привязок.
- `project_parameters(doc)` → `имя -> {u"instance": set(id категорий), u"type": set(...)}`
  по `doc.ParameterBindings`.
- `project_options(doc, category_ids=None, instance_only=False)` → готовые строки для
  `pp_param_picker`: `ADSK_Группирование    · экземпляр / тип · 2 категории`.
  `category_ids` меняет хвост на «есть у N из M выбранных категорий» и поднимает полные наверх.
  `instance_only=True` убирает привязки типа — для инструментов, читающих значение через
  `LookupParameter` у экземпляра (иначе показали бы параметр, который они не прочитают).
- Встроенные и семейные параметры в привязки НЕ попадают: поэтому кнопка «Выбрать…» всегда
  дополняет поле ручного ввода, а не заменяет его.

---

## Панели и инструменты

Карточки сгруппированы по панелям. Шаблон карточки инструмента — в конце файла.

### 01_Инфо
- **Инфо** — информация о плагине (`script.py`, `icon.png`, `logo.png`). Справочная кнопка.

### 02_Настройки
- **Настройки** — UI настроек плагина. Читает/пишет через `pp_settings`. Точка управления
  всеми параметрами из `pp_settings.json`. Вкладка «Проверка» содержит настройки инструментов
  панели проверки, включая показ pyRevit-отчёта для передачи параметров по MEP.

### 03_Марки
Панель работы с марками. Опирается на `pp_tags`, `pp_leaders`, `pp_geometry`. Два стека.
Порядок кнопок задан тремя файлами сразу (`bundle.yaml` с ключом `layout:`, `_layout`, `layout`) —
имена в них = имена папок С числовым префиксом, без расширения; не перечисленные элементы не создаются.
Текущий порядок (после переноса «Сопоставить труб и возд.» к группе «Сопоставить», 2026-06-30):
- **01_Сопоставить** — сопоставление марок.
- **02_stack2.stack**: Сопоставить по семейству / Сопоставить по типу.
- **03_Сопоставить_марки_воздуховодов_труб** — перенесена сюда (была 11-й).
- **04_Копировать по типу**, **05_Копировать по семейству**.
- **06_Копировать марки для пары труб** — учитывает `pipe_pair_distance_mm`.
- **07_Копировать марки труб**.
- **08_Применить тип марки от эталона**.
- **09_stack3.stack**: Применить тип марки по типу / по семейству (переименован из stack2 в stack3 — имена стеков в одной панели лучше держать уникальными).
- **10_Копировать вертикальные воздуховоды и трубы авто**, **11_… (ручной)**.
- **12_Инструменты меток** — конфигуратор шаблона `tag_rules` (WinForms). Три зоны:
  список категорий (пока 2 — `CONFIG_ENUM_NAMES`: воздухораспределители, арматура возд.),
  `TreeView` семейств→типов ЭТОЙ категории из проекта (`collect_element_types`), справа
  `ComboBox` существующих марок категории (`collect_tag_symbols`). Кнопки «Назначить
  выделенному»/«Очистить» пишут правило на узел (семейство → `type=None`, тип → конкретный).
  Экспорт/импорт правил отдельным `.json` (сервер, брату). Сохраняет в `pp_settings.json`.
  Правила ДРУГИХ категорий в списке не трогаются (сохраняются как есть).
  С 2026-08-20 окно на WPF и с двумя режимами (чипсы под заголовком):
  «По семейству и типу» — прежнее дерево (уже БЕЗ воздуховодов); «Воздуховоды» — пресеты
  `tag_size_presets`. Во вкладке «Воздуховоды» три колонки: слева список пресетов с полем
  имени и кнопками «Создать/Переименовать/Удалить», в центре строки активного пресета и
  строка покрытия («не покрыты надписи длиной 5–6, 8» цветом `Hot`), справа редактор строки
  (на какие воздуховоды — все/семейство/тип, «от»/«до» знаков, марка) и блок `Card.Result`
  с предпросмотром и примером надписи такой длины. Диапазоны пересекаться не могут ВНУТРИ
  одной области; если сосед начинается раньше и упирается в новый диапазон — окно само
  подвигает его верхнюю границу и пишет об этом в статус.
  `ask_tag_rules(rules, categories, load_marks, load_types, size_presets, size_active)`
  возвращает словарь `{"rules", "presets", "active"}` (раньше — просто список правил).
  Экспорт пишет `{"rules", "presets"}`; импорт понимает и старый формат (голый список).
- **14_Применить по шаблону** — ГЛАВНЫЙ рабочий сценарий. Пользователь ставит марки родной
  командой Revit «Маркировать по категории», затем жмёт эту кнопку — скрипт находит марки
  (если что-то выделено — по выделению, иначе весь активный вид), по помеченному элементу ищет
  правило (`find_rule_for_element`) и меняет тип марки `ChangeTypeId`. По сути «Применить тип
  марки», но источник — шаблон, а не эталон. Отчёт: изменено / уже по шаблону / нет правила
  (точные имена) / тип марки не загружен. После применения отдельный блок покрытия показывает
  долю обработанных элементов, для которых подобралось правило, семейства и типы без
  применимого правила и указанные в найденных правилах типы меток, не загруженные в проект.
  Наличие типа марки не влияет на процент покрытия: это отдельная проблема конфигурации.
  С 2026-08-20 сначала пробуется размерное правило активного пресета (`tag_size_active`):
  для воздуховодов читается `IndependentTag.TagText`, считается длина надписи и берётся
  строка пресета, подходящая по семейству/типу воздуховода и по длине. Старые правила
  `tag_rules` по воздуховодам вычищаются `strip_size_categories`; для остальных категорий
  всё как было. Если диапазон не подошёл — падаем на правило по семейству, если и его нет —
  марка пропускается. В отчёт добавлены: «из них по размеру надписи», активный пресет,
  «нет строки под такую длину надписи» и счётчик марок с нечитаемой (пустой) надписью.
  > Кнопку «13_Маркировать» размерные правила пока не трогают: она ставит марку, когда
  > надписи ещё нет. После расстановки достаточно прогнать кнопку 14.
  > Кнопка «13_Маркировать по правилу» (своя постановка по клику через `IndependentTag.Create`)
  > удалена как избыточная 2026-07-01 — рабочий путь только через родную команду + кнопку 14.
  > Бэкап удалённой кнопки: `BACKUPS/Маркировать по правилу_удаление_2026-07-01_17-19`.
- **«С плана» в окне «Шаблон марок»** (2026-08-21) — в шапке окна, в режиме «По семейству и
  типу», две кнопки: «Скопировать для типа» и «Скопировать для семейства». Окно закрывается,
  пользователь показывает марки в модели, окно открывается снова с тем же состоянием и
  разворачивает дерево на записанном узле. Разбор образца — `lib/pp_tag_sample.py`
  (`collect` / `apply` / `summary` / `report_lines`). Правило собирается из помеченного
  элемента (категория/семейство/тип) и типа самой марки (`tr.symbol_names`) и пишется в
  `tag_rules` через `set_assignment`. Воздуховоды пропускаются: у них своя вкладка.
  > `PickObject` из обработчика модального окна (даже после `Hide()`) вешает Revit —
  > поэтому окно закрывается и `script.py` крутит цикл «показал окно → выбор → показал снова».
  > Отдельная кнопка «15_Взять марку с плана» существовала несколько часов и удалена
  > 2026-08-21 как лишняя: то же самое делают кнопки внутри окна. Бэкап —
  > `BACKUPS/Взять марку с плана_удаление_2026-08-21_15-37`.
- **15_Раздвинуть марки** (2026-08-31) — разводит наложившиеся марки активного вида.
  Ядро в `lib/pp_tag_layout.py`, окно — `ui.xaml` + `pp_spread_window.py`. Три области
  («Все на виде», «Только выделенные», «По категориям»), три направления
  («Автоматически» по меньшему перекрытию, «Только по вертикали», «Только по горизонтали»),
  зазор и максимальное смещение в миллиметрах НА ЛИСТЕ, галочки по выноскам.
  Категории берутся не из `ALLOWED_CATEGORIES`, а собираются с вида по категориям САМИХ
  марок — список всегда соответствует тому, что на виде.
  > Предпросмотр («разъедется 39 марок из 47») считается в окне: `solve` — чистая
  > математика без Revit API. Скрипт применяет уже посчитанные смещения, поэтому
  > пользователь получает ровно то, что видел. Ввод в полях пересчитывается через
  > `DispatcherTimer` с задержкой 260 мс.
  > Ни один проход решателя не может ухудшить картину: лучшая раскладка запоминается,
  > при равном счёте берётся более ранняя (меньше сдвинутых марок). Если разрешённого
  > смещения не хватило, марки остаются на месте и после работы выделяются в модели.
  > Закреплённые марки и марки внутри групп не двигаются, но считаются препятствиями.

  Галочка «Разбирать пересечения выносок» (2026-08-31, по итогам первого запуска в модели)
  включает два разных механизма. Перекрестившиеся выноски раздвижкой НЕ лечатся: крест
  означает, что порядок марок не совпадает с порядком их элементов, поэтому марки меняются
  местами (`_untangle`), если обеим хватает разрешённого смещения и если бед вокруг стало
  меньше. Отдельно марку выталкивает с чужой выноски (`_leader_obstacles` + `segment_push`).
  > Перестановка примеряется только к парам, у которых беда есть прямо сейчас, и общее
  > число примерок ограничено `MAX_SWAP_ATTEMPTS`: без этих двух оговорок предпросмотр на
  > плане в 300 марок думал секундами.
  > Перестановка принимается и при РАВНОМ весе, если стало меньше крестов. Без этой
  > оговорки на столбике марок распутывание вставало намертво: вес креста уходил, но
  > ровно на столько же прибавлял проход выноски сквозь соседний текст.

### ~~04_Опуск~~ (панель удалена 2026-07-22)
Панель распущена: держать отдельную группу под два инструмента смысла не было. Оба инструмента перенесены в начало панели «Рабочие инструменты» — см. её раздел.
- **Опуск_трассы** — построение опуска трассы. Использует `pp_geometry`. Теперь `05_Рабочие инструменты.panel/01_Опуск_трассы.pushbutton`.
- **Соединить_углом** — теперь `05_Рабочие инструменты.panel/02_Соединить_углом.pushbutton`.

### 05_Теплопотери
Расчёт/проверка теплопотерь и связанные окраски.
- **01_Нарезка полов через пространства**, **02_Нарезка кровли через пространства**.
- **03_Перенос_данных_из_пространств**.
- **04_Окрашивание для проверки теплопотерь**.
- **05_Окрашивание для проверки стороны света**.
- **06_Окрашивание для проверки типа стен**.
- **07_Определить_сторону_света** — учитывает `detect_orientation`.
- **08_Окрасить задвоение**.
- **Объединить стены** — собирает куски одной стены в одну (см. карточку ниже).
> Отчёты управляются `heatloss_show_report`.

### 06_Оформление коллекторов
- **01_Создать_3D_вид_коллектора**.
- **02_Создать_разрез_из_3D_вида** — создаёт разрез по Section Box активного 3D-вида с одной из четырёх сторон (`front` / `back` / `left` / `right`). Модальное WPF-окно (`ui.xaml` + `pp_section_window.py`) показывает крупную прозрачную 3D-коробку: выбранная грань получает акцентную заливку и обводку, стрелка показывает направление взгляда внутрь, а подпись под схемой явно называет выбранную грань. В этом же окне можно назначить шаблон вида разреза или выбрать вариант «Без шаблона вида». Последний подтверждённый выбор хранится в `pp_settings.json` под ключом `create_section_from_3d_template` (идентификатор + имя; при открытии другого проекта возможен запасной поиск по имени) и автоматически подставляется при следующем запуске. Для выбора двух параметров используется `ask_options(...)`, прежняя публичная `ask_side()` и формат её возврата сохранены. Перед назначением шаблон проверяется через `IsValidViewTemplate(...)`. Предварительные ошибки и итог выводятся через `pp_wpf.show_report`.

### 07_Прокси-контроллер
Работа с «прокси-позициями». Использует `proxy_rule_codes` и `proxy_position_types` из настроек.
- **01_Найти прокси-контроллер**.
- **02_Создать_прокси_позицию** — создаёт рядом с контроллером экземпляр семейства `PP_Прокси позиция 1`, заполняет параметры `ADSK_*`, код правила, тип позиции, GUID и признак создания плагином. Основное окно ввода переведено с WinForms на модальный WPF по общей теме: `ui.xaml` + `pp_proxy_window.py`, карточки основных параметров и классификации, живой статус и проверка положительного количества. Функция `ask_proxy_data(rule_codes, position_types)` возвращает прежний словарь строк с ключами `ADSK_*`, `PP_Код правила`, `PP_Тип позиции`; десятичная запятая количества нормализуется в точку. Ошибки показываются через `pp_wpf.show_report`, итог — через `pp_settings.show_report` с учётом пользовательских настроек отчётов.

### 05_Рабочие инструменты
Панель-«сборник» рабочих инструментов моделирования (создана 2026-07-22). Имя в ленте задаётся `bundle.yaml` панели (`title: "Рабочие инструменты"`) — без этого файла pyRevit показал бы префикс номера (см. правило в `AGENTS.md`).

История перестроек 2026-07-22: панель создана между «Прокси-контроллер» и «Проверка»; затем сюда перенесён инструмент передачи по MEP из «Проверки»; затем панель «Опуск» распущена — оба её инструмента перенесены в начало этой панели, сама панель «Опуск» удалена, нумерация панелей закрыта без пропусков (итог: 01_Инфо … 05_Рабочие инструменты, 06_Проверка … 11_Статистика). Номера панелей нестабильны — код не должен на них завязываться.

Порядок кнопок (по префиксу папки, `layout` не используется):
- **01_Опуск_трассы** («Опуск подъем углы») — перенесён из панели «Опуск» 2026-07-22, логика без изменений.
- **02_Соединить_углом** («Соединить под углом») — перенесён из панели «Опуск» 2026-07-22, логика без изменений. Приёмы работы с коннекторами из него переиспользованы в «Выровнять уклон».
- **03_Выровнять уклон по эталону** — приводит выбранные трубы к одной высоте, убирает уклон, выравнивает систему в горизонтальной плоскости. Первая версия — только трубы (`OST_PipeCurves`) и их фитинги. Сценарий: сначала `PickObject` трубы-ЭТАЛОНА, затем `PickObjects` труб для выравнивания. Целевая Z = средняя Z коннекторов эталона (для горизонтального эталона = его ось); если эталон сам с уклоном — спрашивает подтверждение на среднюю высоту. Алгоритм внутри одной транзакции: собрать сеть (фитинги, подключённые к выбранным трубам), разделить их на «безопасные» (все соседи внутри выборки) и «внешние» (есть связь наружу — например отвод-опуск к стояку); запомнить внутренние пары коннекторов по `(ownerId, Connector.Id)`; отсоединить их; каждую трубу перестроить в горизонтальный `Line` на целевой Z (XY концов сохраняются, уклон исчезает); безопасные фитинги сдвинуть по Z через `ElementTransformUtils.MoveElement` на dZ = target−средняяZ; восстановить соединения `ConnectTo` (совпадение точек в пределах 1 мм). Внешние фитинги НЕ трогаются, показываются в отчёте для ручной доводки. Предупреждения/ошибки Revit гасятся `IFailuresPreprocessor` (`DeleteAllWarnings` + `ResolveFailure`) — модальные окна не всплывают. Подробный лог в `_logs/flatten_slope.log`. Воздуховоды планируется добавить позже. Связанная логика соединений — в «Соединить под углом» (кнопка 02 этой же панели). Проверено в Revit 2026-07-22: работает, модель не ломает.
- **04_PP_Передача параметров по MEP-соединениям** — перенесён сюда из «Проверки» 2026-07-22. Логика без изменений; настройка `mep_transfer_show_pyrevit_report` по-прежнему на вкладке «Проверка» в «Настройках», ключ конфига `mep_param_transfer_settings`. Полное описание — в карточке в разделе «Проверка».
- **05_Опуск_Подъем_элементов** — простое вертикальное перемещение смешанной выборки MEP-элементов одной группой. Поддерживает трубы и воздуховоды (включая гибкие), фитинги и арматуру, воздухораспределители, механическое оборудование, сантехнические приборы и спринклеры. Сначала берёт допустимые элементы из предварительного выделения; если их нет, запускает `PickObjects` с фильтром категорий. Режимы WPF-окна: «Опустить»/«Поднять» на положительную величину в мм и «На отметку» от выбранного уровня. Для абсолютной отметки доступны три опоры всей выборки: медиана осей/точек вставки элементов, общий низ или общий верх. Вся выборка передаётся одним вызовом `ElementTransformUtils.MoveElements`, поэтому взаимное положение сохраняется. Закреплённые элементы и члены групп блокируют запуск; внешние MEP-подключения подсчитываются и показываются как предупреждение. Ключ настроек pyRevit — `pp_element_move` (`mode`, `value_mm`, `elev_mm`, `level_key`, `ref_kind`).

### 08_Проверка
- **01_Проверка спецификации** — окно пакетной проверки спецификации по всему проекту. Первая версия включает независимые правила: пустое `ADSK_Наименования`, пустое/нулевое `ADSK_количество`, запрещенные тексты в `ADSK_Наименование`, пустая/нулевая `ADSK_Толщина металла`, нестандартные размеры медной трубы, отсутствие пары между прокси и исходными элементами по `PP_GUID`, несовпадение имени системы с `ADSK_Группирование` у воздуховодов и связанных категорий и дополнительные проверки сортировки/MEP. Архитектура вынесена в `lib/pp_spec_checks.py`: каждая проверка оформлена как отдельный runner и запись в реестре, поэтому новые правила можно добавлять без переделки логики запуска и окна отчета.
- **Кнопка «Выбрать…» в проверках** (2026-08-19) — у опций с `option_type=u"param"` рядом с полем появилась кнопка, открывающая общее окно `lib/pp_param_picker`. Помечено 24 опции: 20 в `pp_spec_checks.py`, 3 в `pp_model_checks.py` (`pipe_flow_param`, `slope_param`, `position_param`), 1 в `pp_heatloss_checks.py` (`numname_param`). Список даёт ключ `param_provider` в конфиге окна (`provider(option_key, values)`), в скриптах это `pp_param_source.project_options(doc)` — параметры проекта целиком, без привязки к категориям конкретной проверки (у разных проверок категории лежат в разных опциях, а значение многие читают и у экземпляра, и у типа). НЕ помечены намеренно: `badtext_params` (список имён через запятую — кнопка затёрла бы всё поле), `copper_keyword_source` (поле-режим: Имя семейства / Имя типа / параметр / Авто), `proxy_ctrl_family` и `proxy_ctrl_proxy_family` (имена семейств). Значение опции `param` читается как обычный текст, формат сохранённых настроек не изменился.
- **UI/состояние Проверки спецификации** — настройки в окне запуска показываются не общим списком, а только для выбранного правила. Сохраняются в `pp_settings.json` в ключе `spec_check_settings`: конфиг параметров, набор отмеченных правил и последнее выбранное правило. Это сделано для повторных запусков без повторного ручного ввода.
- **Проверка №3 в Проверке спецификации** — поле `bzero_text` теперь хранит список запрещенных фрагментов через `;` (логика ИЛИ): например `b=0 мм; класс герметичности ,`. Запятая не является разделителем, потому что может входить в искомый фрагмент. Поиск нормализует лишние пробелы и пробел перед запятой, а старые сохраненные значения `b=0`/`b=0 мм` мягко заменяются на новый список по умолчанию.
- **Медные трубы в Проверке спецификации** — правило нестандартных размеров имеет отдельные настройки: `copper_keyword_source` (где искать ключевые слова; поддерживает `Имя семейства`, `Имя типа`, `Имя элемента`, `Категория`, имя любого параметра или `Авто` для старого широкого поиска), `copper_keywords`, `copper_size_param` (какой размер сравнивать со стандартами; по умолчанию `Диаметр`) и `copper_standard_sizes_mm`.
- **Отчет Проверки спецификации** — окно отчета модельное (`form.Show()`), скрипт использует `__persistentengine__ = True`, хранит открытые окна в `OPEN_REPORT_WINDOWS` и выделяет элементы через `ExternalEvent`. При нажатии «Выделить» фокус сначала возвращается в Revit через безопасный `Microsoft.VisualBasic.Interaction.AppActivate` без `DllImport`, затем короткий `Timer` поднимает `ExternalEvent`. В обработчике сначала вызывается `ShowElements`, затем последним действием `Selection.SetElementIds`. Это нужно, чтобы палитра свойств обновлялась на выбранный проблемный элемент, а не оставалась на старом выборе.
- **03_Проверка Модели** — движок `lib/pp_model_checks.py` (реестр `CheckDefinition` + `runner(doc, config, cache)`, `ElementCache`, опции `text`/`category_multiselect`, окно само подхватывает новые проверки). Проверки (2026-07-24): **1** Трубы без расхода; **2** Уклон у труб/возд.; **3** Элементы без системы (висящие); **4** Почти соединённые концы — пары свободных End-коннекторов ближе `near_tolerance_mm` (50 мм по умолч.), но не соединённых; поиск через пространственную сетку с шагом = допуску (27 соседних ячеек), пары одного элемента и уже соединённые пропускаются, каждая пара выводится один раз; **5** Короткие обрезки — трубы/возд. длиной `LENGTH_EPS_MM < L < short_segment_mm` (100 мм), категории `short_segment_categories` (по умолч. трубы+возд.; ещё гибкие); **6** Вырожденная геометрия — осевые с `L <= degenerate_tolerance_mm` (1 мм), категории фиксированы (трубы/возд./гибкие); **7** Дубли в одной точке — наложения по категориям `duplicate_categories` (по умолч. оборуд.+арматура труб/возд.+трубы+возд.), точечные семейства по точке вставки, осевые (трубы/возд./гибкие) по совпадению ОБОИХ концов (направление не важно, концы упорядочены), допуск `duplicate_tolerance_mm` (10 мм), `duplicate_same_type` (да) требует совпадения типоразмера. Единицы Revit — футы, пороги в мм через `FEET_TO_MM=304.8`. Коннекторы берутся у осевых через `element.ConnectorManager`, у семейств через `MEPModel.ConnectorManager` (хелпер `_get_connector_manager`). Ключ настроек в `pp_settings.json` — `model_check_settings` (config/selected_keys/last_selected_key). Окно модельное (`form.Show()`, `__persistentengine__`), выделение/окраска через `ExternalEvent`.
- **04_Аудит модели** (2026-08-17) — обёртка над штатными предупреждениями Revit (`doc.GetWarnings()`): собирает нерешённые предупреждения, группирует по `GetDescriptionText()`, считает повторы. Revit API 2022 не отдаёт фактические дату/время создания предупреждения, поэтому Аудит сохраняет в настройках кнопки pyRevit дату и время **первого обнаружения** каждой записи (ключ: текст, серьёзность, id связанных элементов). В левой таблице есть колонка «Впервые замечено»; группы отсортированы так, что сверху находятся содержащие самое недавно впервые обнаруженное предупреждение. При первом запуске все уже существующие предупреждения получают одну исходную отметку; после устранения запись удаляется из локальной истории, и повторное появление считается новым. Справа id элементов группы (`GetFailingElementIds()` + `GetAdditionalElementIds()`, дедуп; старые варианты имён методов оставлены как запасной путь). Кнопки «Выделить все по группе»/«Выделить строку» (тот же паттерн `ExternalEvent` + `Timer` + `AppActivate`, что в «Проверке Модели»/«Проверке спецификации»), «Обновить» (пересбор без перезапуска), «Экспорт в CSV» (utf-8-sig, `;`, с колонкой даты). Окно `form.Show()` + `__persistentengine__`, открытые окна в `OPEN_AUDIT_WINDOWS`. Своя телеметрия `pp_usage.log` добавлена. Кнопка в панели «Проверка» с номером `04_`, иконка — синий треугольник-предупреждение + зелёная лупа (по `ICONS.md`). Смысл: адаптация встроенного «Просмотр предупреждений» под рабочий процесс (группировка, счётчики, быстрый переход, экспорт).
- **02_Окрасить рандомно** — окраска активного вида случайными цветами по значению любого параметра: элементы с одинаковым значением получают общий цвет. Окно — `lib/pp_paint_dialog` (категории с активного вида + поле «ПАРАМЕТР ГРУППИРОВКИ» + галочка сброса). Выбор категорий и имя параметра сохраняются в конфиге pyRevit (`selected_cats`, `group_param`), по умолчанию `ADSK_Группирование`. Значение читается `LookupParameter` у ЭКЗЕМПЛЯРА (параметры типа не читаются), элементы без значения получают сброс переопределений. Цвета — золотое сечение по тону (137.508°) плюс перемешивание порядка групп, чтобы соседние значения были контрастны. 2026-08-19 рядом с полем появилась кнопка «Выбрать…» (`lib/pp_param_picker`): список собирается из двух источников — имена параметров экземпляра с элементов вида (выборка до 60 элементов на категорию, копятся ещё и по категориям в `by_cat[id]["params"]`) и параметры проекта из `doc.ParameterBindings`. Привязки ТИПА в список не попадают намеренно: окраска их всё равно не прочитает. В каждой строке видно, откуда параметр и у скольких из отмеченных категорий он есть; сверху — покрывающие все отмеченные. Список строит `provider`, который `pp_paint_dialog` вызывает в момент нажатия кнопки, поэтому он подстраивается под уже отмеченные категории. Встроенный список под полем оставлен: кнопка его дополняет, а не заменяет.
- **Коллизии** — проверка коллизий.

- **02_PP_Передача параметров по MEP-соединениям** (⚠ 2026-07-22 перенесён в панель «06_Рабочие инструменты»; описание оставлено здесь) — универсальный перенос параметра между подключенными MEP-элементами. В окне инструмента пользователь выбирает категории источника и приемника из четырех вариантов: трубы, соединительные детали трубопроводов, арматура трубопроводов и оборудование, затем задает имя параметра источника и приемника. Рядом с обоими полями есть кнопка «Выбрать…» (2026-08-19 окно выбора вынесено в общий `lib/pp_param_picker`, локальный `parameter_picker.xaml` из папки кнопки убран в BACKUPS): список строится из `doc.ParameterBindings` и показывает только параметры проекта, связанные хотя бы с одной выбранной категорией; полное покрытие выбранных категорий поднимается вверх списка. Для источника доступны привязки экземпляра и типа, для приемника — только экземпляра, потому что инструмент не изменяет параметры типоразмера. Ручной ввод сохранен для встроенных и семейных параметров, отсутствующих в параметрах проекта. Режим обработки выбирается из четырех вариантов: «Вся модель», «Текущий вид», «Источник вручную», «Приёмник вручную». При ручном выборе источника Revit разрешает выбрать только элементы выбранных категорий источника; дальше приемники собираются только из напрямую подключенных к этим источникам элементов выбранных категорий приемника. При ручном выборе приемника Revit разрешает выбрать только элементы выбранных категорий приемника; источники при этом ищутся по их коннекторам. Скрипт смотрит только напрямую подключенные элементы через коннекторы Revit и переносит значение, если найден один источник или несколько источников с одинаковым значением. Если найдены разные значения, элемент попадает в конфликт; если заполненный источник не найден, элемент попадает в отчет «источник не найден». Значение источника читается сначала у экземпляра, затем у типа. Запись выполняется только в параметр экземпляра приемника, чтобы не менять сразу все элементы типоразмера. Стартовая конфигурация окна: источник = трубы, приемник = арматура трубопроводов, параметр = `ADSK_Имя системы сокращенное`. Последние имена параметров и выбранный режим сохраняются в `pp_settings.json` в ключе `mep_param_transfer_settings` (`mode`). Подробный вывод в окно pyRevit управляется настройкой `mep_transfer_show_pyrevit_report` на вкладке «Проверка» в инструменте «Настройки»; итоговая белая сводка показывается всегда.

### 09_Оформление 3D видов
- **01_3D перекрытий**, **02_3D между перекрытий**.

### 10_Расчеты
Панель «Расчеты» живёт во второй вкладке (`PP_ОВиК 2.tab`).
- **01_Длина коридора**.
- **02_Расход из пространства** (2026-08-31) — перенос рабочего скрипта Dynamo на
  кнопку. Для каждого выбранного пространства собирает воздухораспределители
  (`OST_DuctTerminal`) внутри него, делит общий расход пространства на их число и
  пишет долю в параметр решётки. Вытяжка и приток разделяются ПО ИМЕНИ СИСТЕМЫ:
  имя начинается с одного из вытяжных префиксов (`В`, `ВЕ`) или приточных
  (`П`, `ПЕ`); имена нормализуются (верхний регистр, пробелы убраны). Системы,
  в имени которых есть слово-исключение (`ДУ`, `ПД`, `ПОДПОР`, …), выбрасываются
  до проверки префикса — иначе противодымные системы попали бы в приток по
  букве «П». Имя системы читается по цепочке: `Имя системы` / `System Name` /
  `ADSK_Имя системы`, затем `BuiltInParameter.RBS_SYSTEM_NAME_PARAM`.
  Принадлежность решётки пространству — как в Dynamo: точка вставки, центр, низ
  и верх габаритного бокса, а если ни одна не попала — те же точки со сдвигом
  вниз на 100…1000 мм (решётки в подшивном потолке НАД пространством). Точки
  считаются один раз на решётку, перед `IsPointInSpace` стоит быстрый отсев по
  габаритному боксу пространства (`maybe_inside`) — без него полный перебор
  «все решётки × все пространства» съедает минуты.
  Пространства берутся из выделения, а если его нет — выбираются на виде
  (`PickObjects` вызывается ПОСЛЕ закрытия окна, см. грабли WPF №19).
  Окно (`ui.xaml` + `pp_airflow_window.py`): имена пяти параметров с кнопками
  «Выбрать…» (`pp_param_picker` + `pp_param_source.project_options`,
  `instance_only=True`), галочка записи количества решёток, под раскрывашкой —
  префиксы систем и список исключений. Настройки хранятся в `pp_settings.json`
  под ключом `space_airflow_settings`, дефолты — в `DEFAULT_CONFIG` скрипта.
  Отчёт (`pp_wpf.show_report`, `monospace=True`) даёт по каждому пространству
  «общий расход / число решёток = доля», а отдельными блоками — решётки с чужой
  системой (не тронуты), решётки, попавшие сразу в несколько выбранных
  пространств (двойная запись, повод проверить границы), и ошибки записи.

### 11_Оформление
- **01_Заполнить_имя_листа_по_видам** — использует `sheet_name_prefixes` из настроек.
- **02_Заполнить_имя_листа_по_планам** — пакетно заполняет «Имя листа» по имени
  размещённого вида.
  - Вход: `forms.select_sheets` (мультивыбор листов).
  - Логика: имя вида `X_<локация>_<раздел>` → split по `_`, префикс отбрасывается;
    `Этаж NN` → `План N этажа` (несколько этажей на листе → `План N, M этажей` +
    предупреждение); нечисловые локации — по `sheet_plan_location_map`; раздел —
    по `sheet_plan_section_map` (иначе как есть); сборка `"{локация}. {раздел}."`.
  - Приоритет: ключ словаря локаций ВАЖНЕЕ числового этажа. Ключ-цифра
    сопоставляется с номером этажа из имени вида, как в имени и нормализованно
    (напр. `99 = План кровли`, `00 = План подвала` перекрывают «Этаж 99/00»).
  - UI: `DataGridView` с колонками галочка / Лист / Было (имя вида) /
    Станет (имя листа, редактируемое) / Примечание; запись одной транзакцией
    через `set_unique_sheet_name` (защита от дублей суффиксом).
  - Параметры: `sheet_plan_location_map`, `sheet_plan_section_map`.
  - Решение: сделан отдельно от «по видам» (разные парсеры); общие хелперы
    (`get_sheet_viewports`, `get_view_names_from_sheet`, `set_unique_sheet_name`)
    пока продублированы в скрипте, вынос в `lib` — на будущее.

- **03_Оси связей в шаблоне** — полуавтомат на UI Automation (внешний
  `uia_worker.ps1`): снимает категорию «Оси» в параметрах отображения связи.
  Revit 2022 не даёт API для покатегорийных настроек связей. Лог:
  `_logs/link_axes_uia.log`.
- **04_Связи в шаблоне** — пакетно задаёт видимость RVT-связей (вкладка
  «Связанные файлы RVT» в В/Г) сразу для многих шаблонов видов ИЛИ обычных видов.
  - Ключевой факт: галочка видимости связи — это обычное скрытие элемента в виде,
    поэтому работает через API (`View.HideElements` / `View.UnhideElements`),
    **в том числе для шаблонов видов** — UIA не нужен. Покатегорийные настройки
    внутри связи (см. инструмент 03) API по-прежнему не поддерживает.
  - **Грабли Revit 2022:** у `View` НЕТ метода `IsElementHidden` (ошибка
    `'View3D' object has no attribute 'IsElementHidden'`). Состояние читается
    через `Element.IsHidden(View)`.
  - Связь выключается ЦЕЛИКОМ: скрывается и `RevitLinkType` (верхняя строка-галочка
    в В/Г), и все его `RevitLinkInstance` (вложенные строки). Если скрыть только
    экземпляры, верхняя галочка в диалоге остаётся стоять. Группировка — класс
    `LinkEntry` (тип + экземпляры), правый список = один пункт на файл связи.
  - **Модель UI (важно, была переделана):** слева — «к чему применяем»
    (`TabControl`: вкладки «Шаблоны видов» и «Виды»), справа — ЖЕЛАЕМОЕ СОСТОЯНИЕ
    связей, а не выбор: галочка стоит → связь видна, галочка снята → связь
    скрывается. Первая версия трактовала правый список как «выбор связей» и
    ругалась «Не выбрана ни одна связь» — так неправильно.
  - Вкладка «Виды» нужна для видов, где шаблон = «нет» (иначе каждый вид вручную).
    Кнопка «Текущий вид» отмечает активный вид. Если у обработанного вида назначен
    шаблон, он попадает в отчёт с предупреждением, что шаблон перебьёт изменения.
  - Кнопка «Как в текущем виде» подтягивает фактическое состояние связей активного
    вида в правый список (и это же состояние — дефолт при первом запуске).
  - Класс `FilteredCheckList` — переиспользуемая пара «строка поиска +
    `CheckedListBox`»; галочки при фильтрации не теряются (набор имён в
    `checked_names`, `sync()` перед перезаполнением).
  - Имена: только через `Element.Name.GetValue(el)` (`elem_name`), иначе у
    `RevitLinkInstance` вылезает длинное `файл.rvt : 12 : позиция ...`.
    Подписи связей — имя `RevitLinkType`, дубли уточняются `(экз. N)`.
  - Фильтр видов/шаблонов: исключены Schedule, ColumnSchedule, PanelSchedule,
    DrawingSheet, Legend, Report, SystemBrowser, ProjectBrowser, Internal, Undefined.
  - Конфиг pyRevit: `saved_templates`, `saved_views`, `saved_visible_links`.
  - Всё одной транзакцией; проблемные виды и виды с шаблоном — в отчёт output.
  - Итоговое окно — через `show_report` из `pp_settings`, как у остальных
    инструментов (подчиняется галочкам «Настройки → Общие → Уведомления»).
    Оставшиеся `forms.alert` — только блокирующие сообщения (нет связей, ничего
    не отмечено), это не отчёты.
  - **Грабли:** `script.get_output()` + любой `print_md` открывают окно pyRevit
    независимо от `show_report`. Поэтому `get_output()` вызывается лениво — только
    если есть ошибки/диагностика или в настройках включён `show_success_report`.
    Держать `output = script.get_output()` на уровне модуля нельзя.

### 11_Статистика
- **01_Статистика использования** — счётчик запусков инструментов плагина.
  - **Механизм: СВОЯ телеметрия (`lib/pp_usage.py`), встроенная телеметрия pyRevit
    больше не используется.** Отказались от неё 2026-07-20: она создавала файл на
    каждую сессию (67 файлов), но реальных записей почти не писала, логировала чужой
    `pyRevitCore`, а путь к папке сохраняла экранированным и молча отключалась.
  - Универсального хука «на любую кнопку» в pyRevit НЕТ (есть только
    `command-before-exec[<id>]` по конкретному id), поэтому вызов явный: в начало
    каждого `script.py` вставлена защищённая строка

        try: import pp_usage; pp_usage.log(__file__)
        except Exception: pass

    Она стоит сразу после строки кодировки. `pp_usage.log()` никогда не бросает
    исключений — сбой логирования не должен ронять инструмент.
  - **Формат: append-only JSONL** — `telemetry/pp_usage_YYYY-MM.jsonl` (одна
    JSON-запись на строку, файл на месяц). Выбран намеренно: дописывание строки
    атомарно, несколько сессий Revit не портят файл, битая строка не убивает весь
    лог (в отличие от единого JSON-массива, который вёл pyRevit).
  - Поля записи: `ts` / `date` / `time` (локальное время), `tool` (имя без префикса
    «06_»), `bundle`, `panel`, `tab`, `user` (**имя пользователя из настроек Revit**,
    через `HOST_APP.username`), `winuser`, `revit`, `doc`.
  - Разбор пути кнопки идёт вверх по дереву и корректно понимает кнопки внутри
    `.stack` (панель находится через уровень).
  - Кнопка статистики читает `pp_usage.read_records()` и показывает WinForms
    `ListView` с переключателем группировки: по инструментам / по пользователям /
    по инструментам и пользователям. Есть «Экспорт в CSV» (utf-8-sig, разделитель `;`).
  - **Правило для новых кнопок:** при создании любой новой кнопки обязательно
    добавлять строку `pp_usage.log(__file__)` — иначе её запуски не попадут в учёт.
  - **Хранение:** `telemetry/` внутри расширения; периодическая выгрузка на сервер
    (xpenology) — `export_telemetry.bat` (robocopy по маске `pp_usage_*.jsonl`) в
    корне расширения, ставится в Планировщик задач Windows (заменить `DST`).
  - Учёт ведётся с момента установки этой версии; задним числом данных нет.
  - **Грабля чтения (2026-07-24):** нельзя читать JSONL лениво через
    `for line in io.open(path, encoding="utf-8")` — в IronPython построчное чтение
    utf-8 с кириллицей срывается посреди файла с исключением. Если это исключение
    ловить на уровне ВСЕГО файла, теряются все строки после сбоя (симптом: в
    статистике видно только первые 1–2 записи, хотя в файле их сотни). Правильно:
    читать файл целиком в бинарном режиме (`io.open(path, "rb").read()`), декодировать
    самим (`raw.decode("utf-8-sig", "replace")`), бить на строки в памяти и парсить
    каждую в отдельном try/except. Сделано в `pp_usage._read_text` / `read_records`.
  - **Важно:** новая панель/кнопка появится в ленте только после
    `pyrevit caches clear --all` + запуск Revit (Reload не пересобирает ленту).
    Правки внутри `script.py` подхватываются сразу, без Reload.

## ИИ Агент (отдельная вкладка)

Вторая вкладка расширения `ИИ Агент.tab`, панель `01_Обмен`. Канал обмена
данными между Revit и ИИ-агентом для расчёта теплопотерь. Файлы обмена лежат
в `<корень плагина>/AI_обмен/` (папка вне `.tab`, pyRevit её не парсит).
Схема обмена описана в `AI_обмен/README.md`.

- **01_Выгрузить** — читает всю модель (без транзакции) в
  `AI_обмен/model_export.json`. Собирает: пространства (`OST_MEPSpaces`:
  номер, имя, уровень, площадь, температура, точка, bbox, контур-полигон через
  `GetBoundarySegments`), стены (линия/ширина/нормаль/`is_curtain`/площадь),
  окна+двери (точка, `host_id`, площадь), перекрытия (bbox, площадь). У
  ограждающих элементов выгружаются 5 параметров теплопотерь (те же, что пишет
  «03_Перенос данных из пространств»). Координаты в мм, площади в м². Параметр
  в JSON = `{storage, value(сырое), text(display)}`. Предупреждает, если
  выключен расчёт объёмов.
- **02_Загрузить** — читает `AI_обмен/commands.json` (если нет — диалог выбора
  файла), пишет параметры по `id` элементов одной транзакцией. Формат команды:
  `{"id":int, "params":{"Имя":value}}`. Значения — display; для Double сначала
  `SetValueString` (единицы проекта), при неудаче `Set(float)`. Пишутся только
  параметры экземпляра, read-only пропускаются. Отчёт: элементов/параметров
  записано, пропущено, ошибки (подробности в окно pyRevit).
- Общий путь берётся через `get_extension_root()` из `pp_settings`.
- **Важно:** новая вкладка/кнопки появятся только после `pyrevit caches
  clear --all` + запуск Revit (Reload не создаёт ленту с нуля). Первая версия
  (2026-07-07), договорённость: выгрузка = вся модель, формат = JSON,
  загрузчик пишет любые параметры (включая произвольные).

---

## Шаблон карточки инструмента

При углублённой проработке инструмента заполнять по образцу:

```
### <Панель> / <Инструмент>
- Назначение: что делает кнопка для проектировщика.
- Вход: что выделяет/выбирает пользователь, какие категории/параметры.
- Логика: ключевые шаги алгоритма (кратко).
- Модули lib: какие функции из lib используются.
- Параметры: какие ключи pp_settings.json влияют на поведение.
- Нюансы/решения: подводные камни, принятые компромиссы, особенности Revit API.
```

---

## Принятые решения и нюансы

В WinForms-окнах размер задавать через `ClientSize`, а НЕ через `Size`.
`Size` — это размер окна целиком, включая заголовок и рамки (~40 px по высоте),
поэтому элементы, позиционированные у нижнего края (кнопки, подписи), обрезаются
границей окна. Правильно: `self.ClientSize = Size(w, h)` и все координаты считать
от `w`/`h`, например `btn_y = h - PAD - BTN_H`. `MinimumSize` при этом остаётся
размером окна — его берут с запасом. Наступили на это в «Статистика использования»
(2026-07-20), кнопки «Экспорт в CSV» и «Закрыть» оказались срезаны.


- Резервное копирование перед изменениями — обязательно (см. `AGENTS.md` → «Резервное копирование»).
- При правке Revit API проверяется вручную через pyRevit Reload.
- **Перестановка кнопок в панели (важно, проверено 2026-06-30):** порядок применяется только при создании ленты с нуля. pyRevit Reload создаёт кнопки с `update_if_exists=True` — обновляет существующие «на месте», НЕ переставляя; `sort_pyrevit_ui` переупорядочивает только панели, не кнопки внутри панели. Переименование папки кнопки двигает её при Reload (меняется unique_name → пересоздаётся), но переименование стека/правка `layout` — нет.
- **Главный нюанс:** структура расширения кэшируется pyRevit в `%APPDATA%\pyRevit\`. После правок раскладки (layout, стеки) одного Reload и даже полного перезапуска Revit мало — нужно очистить кэш: `pyrevit caches clear --all`, затем открыть Revit. Симптом устаревшего кэша — заголовок панели «прыгает» между `03_Марки` и `03 Марки`.
- Стек (`*.stack`) объединяет 2 кнопки вертикально — соблюдать при добавлении кнопок в группу.
- Иконки — строго по `ICONS.md` (32×32, PNG, прозрачный фон, имя `icon`).


### Теплопотери / Объединить стены
- Назначение: архитектор рисует одну стену кусками (простенок, перемычка над проёмом,
  сегменты в ряд), и в расчёте теплопотерь каждый кусок даёт свою позицию. Кнопка собирает
  куски в одну стену.
- Вход: выделенные стены (без выделения — `PickObjects` с фильтром по категории «Стены»).
  Тип объединённой стены выбирается в окне: «Автоматически» (тип самого большого куска
  в наборе) либо конкретный тип из выделения — так объединяются куски с разными именами
  типов, но одинаковым пирогом.
- Логика:
  1. Отсев непригодных кусков: витраж, составная стена, стена в группе, дуга, проёмы
     (`FindInserts`), привязка верха/низа, изменённый профиль. Профиль ловится через
     `SketchId`, а где его нет в API — по числу рёбер самой большой грани стены
     (`_looks_rectangular`: один контур, ровно 4 ребра).
  2. Группировка по плоскости: канонизированное направление оси в плане + смещение линии
     привязки, округлённое до миллиметра.
  3. Внутри плоскости — связные компоненты по касанию гранью (касание одним углом не в счёт).
     Компонента из одного куска уходит в отказы.
  4. Объединение прямоугольников: сетка из всех координат u и v, закрашенные клетки,
     проверка на связность и на дырку, обводка границы в один замкнутый контур, снятие
     промежуточных точек на прямых участках.
  5. Создание: прямоугольник — обычная `Wall.Create` по линии с высотой и смещением,
     любая другая форма — `Wall.Create` по профилю с нормалью самого большого куска.
- Модули lib: `pp_wall_merge` (всё ядро), `pp_wpf`, `pp_settings.show_report`, `pp_usage`.
- Параметры: своих ключей в `pp_settings.json` нет; показ отчёта — общий
  `show_success_report` / `show_warning_report`.
- Нюансы/решения:
  * **Исходные куски удаляются ДО создания новой стены.** Если сначала создать, Revit
    показывает диалог «стены накладываются» посреди работы. Поэтому параметры исходной
    стены снимаются в словарь (`_read_params`) заранее и раскладываются по новой уже
    после создания (`_write_params`).
  * Переносятся только параметры проекта и общие (`Id.IntegerValue >= 0`), плюс явно
    фаза создания/сноса и рабочий набор. Встроенные параметры геометрии не трогаем.
  * Новая стена рождается по осевой линии привязки. Если исходные стояли по грани,
    положение поправляется сдвигом по нормали: сравниваются середины габаритов старой
    и новой стены (`_center_offset`). Это же centering применяется, когда пользователь
    выбрал тип другой толщины.
  * Каждый набор объединяется в своей `SubTransaction`: отказ Revit по одному набору
    не роняет остальные.
  * Стены с проёмами намеренно НЕ объединяются: вместе с куском удалились бы окна и двери.
    Решение пользователя от 2026-09-02 — такие наборы просто уходят в отчёт.
